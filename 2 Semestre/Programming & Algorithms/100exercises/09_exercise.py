salario_atual = float(input('Salário atual: R$'))
aumento = salario_atual * 0.15
novo_salário = salario_atual + aumento
print('Aumento: R${}'.format(aumento))
print('Novo salário: R${}'.format(novo_salário))
